<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="handy.database.mapper_interface.UuidMapper">
    <select id="detailsString">
        select * from uuid_table where uuid = #{param1} LIMIT 1
    </select>

    <select id="detailsUuid">
        select * from uuid_table where uuid = #{param1} LIMIT 1
    </select>
</mapper>
